<div class="page-container">
  <h1 class="page-title">New Review for <%= @recipe.title %></h1>

  <%= render "form", review: @review %>

  <div class="button-row">
    <%= link_to "Back to Recipe", recipe_path(@recipe), class: "btn-link" %>
  </div>
</div>
