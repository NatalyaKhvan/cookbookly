<h1>Dashboard</h1>

<% if @recipes.present? %>
  <h2>Your Recipes</h2>
  <ul>
    <% @recipes.each do |recipe| %>
      <li><%= link_to recipe.title, recipe_path(recipe) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No recipes yet. <%= link_to "Create one", new_recipe_path %></p>
<% end %>
